using system;

class Exemplo
{
    // Atributo estático
    public static string nome = "Maycon";
    // Método estático
    public static void soma(int num1, int num2)
    {
        Console.WriteLine(num1 + num2);
    }
}